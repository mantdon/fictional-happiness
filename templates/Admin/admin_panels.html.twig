{% set home_label = "Home" %}
{% set users_label = "Vartotojai" %}
{% set services_label = "Servisai" %}
{% set ongoing_orders_label = "Einamieji užsakymai" %}
{% set completed_orders_label = "Įvykdyti užsakymai" %}
{% set create_employee_label = "Sukurti darbuotoją" %}

<ul class="nav nav-tabs tab-bar">
    <li class="nav-item">
        {% if home_active is defined %}
            <span class="nav-link active">{{ home_label }}</span>
        {% else %}
            <a class="nav-link" href="{{ path('admin_home') }}">{{ home_label }}</a>
        {% endif %}
    </li>
    <li class="nav-item">
        {% if users_active is defined %}
            <span class="nav-link active">{{ users_label }}</span>
        {% else %}
            <a class="nav-link" href="{{ path('admin_users') }}">{{ users_label }}</a>
        {% endif %}
    </li>
    <li class="nav-item">
        {% if services_active is defined %}
            <span class="nav-link active">{{ services_label }}</span>
        {% else %}
            <a class="nav-link" href="{{ path('admin_services') }}">{{ services_label }}</a>
        {% endif %}
    </li>
    <li class="nav-item">
        {% if ongoing_orders_active is defined %}
            <span class="nav-link active">{{ ongoing_orders_label }}</span>
        {% else %}
            <a class="nav-link" href="{{ path('admin_ongoing_orders') }}">{{ ongoing_orders_label }}</a>
        {% endif %}
    </li>
    <li class="nav-item">
        {% if completed_orders_active is defined %}
            <span class="nav-link active">{{ completed_orders_label }}</span>
        {% else %}
            <a class="nav-link" href="{{ path('admin_completed_orders') }}">{{ completed_orders_label }}</a>
        {% endif %}
    </li>
    <li class="nav-item">
        {% if create_active is defined %}
            <span class="nav-link active">{{ create_employee_label }}</span>
        {% else %}
            <a class="nav-link" href="{{ path('admin_create_employee') }}">{{ create_employee_label }}</a>
        {% endif %}
    </li>
</ul>