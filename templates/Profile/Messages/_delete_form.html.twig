<form method="post" action="{{ path('delete_message', {'id': messageData.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ messageData.id) }}">
    <button class="btn btn-danger">Ištrinti</button>
</form>