{% set vehicles_label = "Užregistruoti automobiliai" %}
{% set orders_label = "Užsakymai" %}
{% set messages_label = "Žinutės" %}
{% set settings_label = "Nustatymai" %}
{% set changepassword_label = "Keisti slaptąžodį" %}

<ul class="nav nav-tabs justify-content-center very-tabs">
    <li class="nav-item">
        {% if vehicles_active is defined%}
            <span class="nav-link active tab-active">{{ vehicles_label }}</span>
        {% else %}
            <a class="nav-link tab-link" href="{{ path('user_vehicles') }}">{{ vehicles_label }}</a>
        {% endif %}
    </li>
    <li class="nav-item">
        {% if orders_active is defined %}
            <span class="nav-link active tab-active">{{ orders_label }}</span>
        {% else %}
            <a class="nav-link tab-link" href="{{ path('user_orders') }}">{{ orders_label }}</a>
        {% endif %}
    </li>
    <li class="nav-item">
        {% if messages_active is defined %}
            <span class="nav-link active tab-active">{{ messages_label }}</span>
        {% else %}
            <a class="nav-link tab-link" href="{{ path('user_messages') }}">{{ messages_label }}</a>
        {% endif %}
    </li>
    <li class="nav-item">
        {% if settings_active is defined %}
            <span class="nav-link active tab-active">{{ settings_label }}</span>
        {% else %}
            <a class="nav-link tab-link" href="{{ path('user_settings') }}">{{ settings_label }}</a>
        {% endif %}
    </li>
    <li class="nav-item">
        {% if changepassword_active is defined %}
            <span class="nav-link active tab-active">{{ changepassword_label }}</span>
        {% else %}
            <a class="nav-link tab-link" href="{{ path('user_changepassword') }}">{{ changepassword_label }}</a>
        {% endif %}
    </li>
</ul>