{% extends 'base.html.twig' %}

{% block title %}Namai{% endblock %}

{% set home_active = 'true' %}

{% block content %}
    {# Seen by unauthenticated users #}
    {% if not app.user %}
        <h1>Hello, world</h1>
        {# Seen by authenticated users #}
    {% else %}
        <h1>Hello, {{ app.user.emailname }}</h1>
    {% endif %}
{% endblock %}